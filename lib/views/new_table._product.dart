import 'package:flutter/material.dart';

class NewTableProduct extends StatelessWidget {
  const NewTableProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}